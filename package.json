{
  "name": "artizen-fyi",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "cron:prod": "wrangler dev --env cron --port 8788",
    "deploy": "wrangler deploy",
    "kv:create": "wrangler kv namespace create CACHE",
    "types": "wrangler types"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "wrangler": "^4.28.0"
  }
}
